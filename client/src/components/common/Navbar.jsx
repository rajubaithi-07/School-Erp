function Navbar() {
  return (
    <header
      style={{
        height: "70px",
        background: "#2563eb",
        color: "#fff",
        display: "flex",
        alignItems: "center",
        padding: "0 25px",
        fontWeight: "bold",
        fontSize: "20px",
      }}
    >
      School ERP
    </header>
  );
}

export default Navbar;